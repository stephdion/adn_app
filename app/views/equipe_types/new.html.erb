<% provide(:title, t(:equipes_type_new)) %>
<div class="margin-bottom">
	<h1 class="text-center"><%= t(:equipe_type_new) %></h1>
</div>

<%= render 'form' %>

<%#= link_to t(:equipe_index), equipe_types_path %>