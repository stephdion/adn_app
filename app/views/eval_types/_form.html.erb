<div class="col-md-6" style="padding-left:0px;">
<%= form_for(@eval_type) do |f| %>
  <% if @eval_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@eval_type.errors.count, "error") %> prohibited this eval_type from being saved:</h2>

      <ul>
      <% @eval_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="span12">
  <div class="span3">
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, :class=>"form-control" %>
  </div>

  <div class="styled-select">
    <%= f.label :organization_id %><br>
    <%= f.collection_select :organization_id, @current_user.organizations, :id, :name, {}, :class=>"form-control" %>
  </div>
  </div>
  </div>

  <div class="span12">
  <div class="span3">
  <div class="actions">
  </br>
    <%= f.submit t(:button_submit), class: "btn btn-default"%>
    <%= link_to t(:general_back), eval_types_path, class: "btn btn-default", :style=>"display:inline;margin-left:10px;" %>
    </br></br>
  </div>
  </div>
  </div>
<% end %>
</div>