require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

describe EvalTypesController do

  # This should return the minimal set of attributes required to create a valid
  # EvalType. As you add validations to EvalType, be sure to
  # update the return value of this method accordingly.
  def valid_attributes
    {}
  end

  # This should return the minimal set of values that should be in the session
  # in order to pass any filters (e.g. authentication) defined in
  # EvalTypesController. Be sure to keep this updated too.
  def valid_session
    {}
  end

  describe "GET index" do
    it "assigns all eval_types as @eval_types" do
      eval_type = EvalType.create! valid_attributes
      get :index, {}, valid_session
      assigns(:eval_types).should eq([eval_type])
    end
  end

  describe "GET show" do
    it "assigns the requested eval_type as @eval_type" do
      eval_type = EvalType.create! valid_attributes
      get :show, {:id => eval_type.to_param}, valid_session
      assigns(:eval_type).should eq(eval_type)
    end
  end

  describe "GET new" do
    it "assigns a new eval_type as @eval_type" do
      get :new, {}, valid_session
      assigns(:eval_type).should be_a_new(EvalType)
    end
  end

  describe "GET edit" do
    it "assigns the requested eval_type as @eval_type" do
      eval_type = EvalType.create! valid_attributes
      get :edit, {:id => eval_type.to_param}, valid_session
      assigns(:eval_type).should eq(eval_type)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "creates a new EvalType" do
        expect {
          post :create, {:eval_type => valid_attributes}, valid_session
        }.to change(EvalType, :count).by(1)
      end

      it "assigns a newly created eval_type as @eval_type" do
        post :create, {:eval_type => valid_attributes}, valid_session
        assigns(:eval_type).should be_a(EvalType)
        assigns(:eval_type).should be_persisted
      end

      it "redirects to the created eval_type" do
        post :create, {:eval_type => valid_attributes}, valid_session
        response.should redirect_to(EvalType.last)
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved eval_type as @eval_type" do
        # Trigger the behavior that occurs when invalid params are submitted
        EvalType.any_instance.stub(:save).and_return(false)
        post :create, {:eval_type => {}}, valid_session
        assigns(:eval_type).should be_a_new(EvalType)
      end

      it "re-renders the 'new' template" do
        # Trigger the behavior that occurs when invalid params are submitted
        EvalType.any_instance.stub(:save).and_return(false)
        post :create, {:eval_type => {}}, valid_session
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested eval_type" do
        eval_type = EvalType.create! valid_attributes
        # Assuming there are no other eval_types in the database, this
        # specifies that the EvalType created on the previous line
        # receives the :update_attributes message with whatever params are
        # submitted in the request.
        EvalType.any_instance.should_receive(:update_attributes).with({'these' => 'params'})
        put :update, {:id => eval_type.to_param, :eval_type => {'these' => 'params'}}, valid_session
      end

      it "assigns the requested eval_type as @eval_type" do
        eval_type = EvalType.create! valid_attributes
        put :update, {:id => eval_type.to_param, :eval_type => valid_attributes}, valid_session
        assigns(:eval_type).should eq(eval_type)
      end

      it "redirects to the eval_type" do
        eval_type = EvalType.create! valid_attributes
        put :update, {:id => eval_type.to_param, :eval_type => valid_attributes}, valid_session
        response.should redirect_to(eval_type)
      end
    end

    describe "with invalid params" do
      it "assigns the eval_type as @eval_type" do
        eval_type = EvalType.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        EvalType.any_instance.stub(:save).and_return(false)
        put :update, {:id => eval_type.to_param, :eval_type => {}}, valid_session
        assigns(:eval_type).should eq(eval_type)
      end

      it "re-renders the 'edit' template" do
        eval_type = EvalType.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        EvalType.any_instance.stub(:save).and_return(false)
        put :update, {:id => eval_type.to_param, :eval_type => {}}, valid_session
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested eval_type" do
      eval_type = EvalType.create! valid_attributes
      expect {
        delete :destroy, {:id => eval_type.to_param}, valid_session
      }.to change(EvalType, :count).by(-1)
    end

    it "redirects to the eval_types list" do
      eval_type = EvalType.create! valid_attributes
      delete :destroy, {:id => eval_type.to_param}, valid_session
      response.should redirect_to(eval_types_url)
    end
  end

end
